http:
  routers:
    traefik-dashboard:
      rule: "Host(`traefik.hl1.xuper.fun`)"
      entryPoints: [websecure]
      service: api@internal
      tls:
        certresolver: letsencrypt
      middlewares:
        - compress@file
        - security-headers@file
